// 💬 Use an arrow function with a ternary operator
const solution = (a, b) => (a.length > b.length ? b + a + b : a + b + a);

const result = solution("45", "1"); // 🥚
console.log(result); // 🐣
